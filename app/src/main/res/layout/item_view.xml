<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/round_corner"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:layout_marginBottom="10dp"
    >

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/title_output"
        android:textSize="20dp"
        android:textColor="@color/black"
        tools:text="My Title" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/content_output"
        android:layout_below="@+id/title_output"
        android:textSize="20dp"
        android:layout_marginTop="10dp"
        android:textColor="@color/black"
        tools:text="My Description" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/time_output"
        android:layout_below="@+id/content_output"
        android:textSize="12dp"
        android:gravity="end"
        android:layout_marginTop="10dp"
        android:textColor="@color/black"
        tools:text="16 March, 2023" />

</RelativeLayout>